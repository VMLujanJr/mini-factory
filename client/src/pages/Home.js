import React from 'react';
import Auth from '../utils/auth.js';

const Home = () => {
    return(
        <main>
            <div className='flex-row justify-space-between'>
                Hello?
            </div>
        </main>
    );
};

export default Home;